import type { InboxResponse } from './wa-inbox';
import type { QueryClient } from '@tanstack/react-query';

/**
 * מפתחות השאילתה וקצב הרענון של תיבת השיחות, במקום אחד.
 *
 * 🔴 **נולד מחשבון שנאכל.** הכפתור הצף והתיבה הפתוחה שאלו כל אחד לחוד
 * את אותה שאלה בדיוק, תחת שני מפתחות שונים, ולכן react-query לא איחד
 * ביניהם ונשלחו שתי בקשות זהות. במדידה של 23/08/2026 יצאו
 * **348 קריאות ל-`api/conversation` בשעה אחת**, שהן 99% מכל תעבורת
 * הפונקציות של כל הפרויקטים בחשבון, מול תיבה שיש בה 23 שורות.
 * מכסת ה-Active CPU החודשית עמדה על 75%.
 *
 * ⭐ **שלושה כללים שמחזיקים את זה:**
 * 1. **מפתח אחד לרשימה.** גם הכפתור וגם התיבה קוראים ל-`inboxKey`, ולכן
 *    כששניהם על אותה לשונית יוצאת בקשה אחת ולא שתיים.
 * 2. **המונה נקרא מהמטמון, לא מבקשה משלו.** כל תשובה של הרשימה נושאת
 *    `counts.waiting` בלי קשר ללשונית, אז לכפתור אין שום סיבה לשאול
 *    בעצמו כשהתיבה כבר פתוחה ושואבת.
 * 3. **השרשור נשאר מהיר.** שם באמת מחכים להודעה, ולכן הוא לא כפוף
 *    לקצב של הרשימה.
 */

/** תחילית משותפת. `invalidateQueries({queryKey:[WA_INBOX_KEY]})` מרענן הכל. */
export const WA_INBOX_KEY = 'wa-inbox';

/** מפתח הרשימה. אותה לשונית ואותו חיפוש = אותו מפתח = בקשה אחת. */
export function inboxKey(tab: 'waiting' | 'all', q: string) {
  return [WA_INBOX_KEY, tab, q] as const;
}

/** מפתח השרשור של לקוח יחיד. */
export function threadKey(phone: string | null) {
  return ['wa-thread', phone] as const;
}

/**
 * ⭐⭐ **הטיימרים כאן הם הרשת השלישית, לא המסלול הראשי (08/09/2026).**
 *
 * 🔴 **המדידה שהובילה לזה:** בשבעה ימים עברו בר.שעל 424 הודעות בסך הכל,
 * בערך 60 ביום, ומולן יצאו **14,077 בקשות ל-`api/conversation` ביממה**.
 * 235 שאילתות על כל אירוע אחד שהוא כן. כתובת IP אחת של המשרד ייצרה
 * 10,621 מהן והמשיכה ב-250 לשעה גם בשלוש לפנות בוקר, כלומר מסך שנשאר
 * דולק. זו הייתה כל צריכת המעבד של החשבון ב-Vercel.
 *
 * ⭐ **מהיום שלוש שכבות, כמו שנבנה ב-07/09 לשאר המסך:**
 *   1. הערוץ החי של סופאבייס (`useRealtimeSync`) דוחף הודעה חדשה **מיד**.
 *      `whatsapp_inbound` ו-`whatsapp_outbound` כבר היו בפרסום החי,
 *      ו-`wa_conversations` הצטרפה במיגרציה `20260908_wa_realtime_freshness`.
 *   2. בדיקת הטריות שרצה ממילא כל דקה מול סופאבייס (`sync_freshness`)
 *      מחזירה עכשיו גם `wa-inbox` ו-`wa-thread`, ותופסת מה שהערוץ פספס.
 *   3. הטיימרים כאן, שפונים ל-Vercel, כרשת אחרונה.
 *
 * 🔴 **ולכן האטת הטיימרים לא מאחרת מידע, היא מקדימה אותו:** היום ההודעה
 * מגיעה תוך עד 30 שניות במקרה הטוב, ומהיום היא מגיעה מיד כשהערוץ חי,
 * ותוך עד דקה כשהוא נפל.
 */

/** קצב הרענון של הרשימה ושל תג ההמתנה, כשיש אדם מול המסך. */
export const WA_INBOX_POLL_MS = 600_000;

/**
 * קצב הרענון של השרשור הפתוח, כשיש אדם מול המסך.
 * ⭐ נשאר הקצר מבין השניים: זה המסך היחיד שבו עובד יושב ומחכה לתשובת לקוח,
 * ולכן הוא מקבל את רשת הביטחון הצפופה ביותר.
 */
export const WA_THREAD_POLL_MS = 120_000;

/**
 * כמה זמן בלי אינטראקציה נחשב "אין אדם מול המסך".
 * ראה `useIdle`. חמש דקות ולא דקה, כדי שקריאת שרשור ארוכה בלי לגעת
 * בעכבר לא תיחשב נטישה.
 */
export const WA_IDLE_AFTER_MS = 5 * 60_000;

/**
 * הקצב כשאין אדם מול המסך.
 *
 * 🔴 **מוריד הילוך ולא עוצר.** מסך שנשאר דולק ככרטיס מצב על הקיר חייב
 * להמשיך להתעדכן, והוא ממילא מקבל את הערוץ החי ואת בדיקת הטריות. עצירה
 * מלאה הייתה מחזירה בדיוק את התקלה של 07/09: מסך פתוח שקפא עד F5.
 */
export const WA_IDLE_POLL_MS = 900_000;

/** הקצב של השרשור כשאין אדם מול המסך. */
export const WA_IDLE_THREAD_POLL_MS = 600_000;

/**
 * כמה זמן הרשימה נחשבת טרייה לצורך רענון בחזרה לחלון.
 *
 * 🔴 **`refetchOnWindowFocus` מרענן רק שאילתה מיושנת**, ולכן בלי הערך
 * הזה ה-`staleTime` הגלובלי (דקה) היה בולע את הרענון בדיוק במקרה
 * שבגללו הוא נוסף: מי שקפץ לטלפון וחזר תוך פחות מדקה.
 *
 * ⭐ חצי דקה לרשימה, ואפס לשרשור. סדר השורות משתנה לאט, ותוכן השיחה
 * הפתוחה הוא מה שאדם באמת מחכה לו.
 */
export const WA_LIST_FOCUS_STALE_MS = 30_000;

/**
 * מספר הממתינים מתוך התשובה העדכנית ביותר של הרשימה, מכל לשונית שהיא.
 *
 * 🔴 **הטרייה ביותר מנצחת, ולא הראשונה שנמצאה.** במטמון יכולות לשבת
 * במקביל תשובות של "ממתינים", של "כל השיחות" ושל חיפוש, וכל אחת נושאת
 * `counts` נכון לרגע שבו היא נמשכה. בחירה שרירותית הייתה מציגה על התג
 * מספר ישן בזמן שהתיבה פתוחה ומעודכנת.
 *
 * מחזיר `null` כשעדיין לא הגיעה שום תשובה, כדי שאפשר יהיה להבדיל בין
 * "אין ממתינים" לבין "עוד לא יודעים".
 */
export function readWaitingCount(client: QueryClient): number | null {
  let newestAt = 0;
  let waiting: number | null = null;

  for (const query of client.getQueryCache().getAll()) {
    if (query.queryKey[0] !== WA_INBOX_KEY) continue;
    const data = query.state.data as InboxResponse | undefined;
    if (typeof data?.counts?.waiting !== 'number') continue;
    if (query.state.dataUpdatedAt >= newestAt) {
      newestAt = query.state.dataUpdatedAt;
      waiting = data.counts.waiting;
    }
  }

  return waiting;
}
