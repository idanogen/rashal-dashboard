/**
 * מה החלונית ממלאת מראש על המסמך שעל המסך: הנושא, סוג המסמך ומספרו.
 *
 * ⭐ עידן, 22/08/2026: "למה בכלל יש שאלה מה מספר המסמך?" צודק. התוסף כבר
 * שולח את כל ערכי השורה, ומספר המסמך נמצא ביניהם. אין סיבה שהעובד יקליד
 * ביד מה שכתוב מולו על המסך.
 *
 * 🔴 **אבל בשורה יש יותר ממספר מסמך אחד.** בתעודת משלוח יושבים גם
 * `SH2603398` (התעודה) וגם `SO2603044` (ההזמנה שממנה היא נוצרה), ושניהם
 * בעלי אותה צורה בדיוק. בחירה לפי צורה בלבד הייתה שולחת ללקוח את מספר
 * ההזמנה בהודעה שכתוב בה "תעודת משלוח מספר".
 *
 * ⭐ **הכלל שמנחה את כל הקובץ: ספק מוליד שדה ריק, לא ניחוש.** שדה ריק
 * העובד ממלא בשתי שניות. מספר שגוי יוצא בשם החברה ללקוח אמיתי.
 */

/**
 * מה שנכתב ב"עדכון בנוגע ל..." לפי המסך.
 * 🟡 מילוי מראש בלבד, והמשתמש עורך. מסך שלא ברשימה מקבל ניסוח כללי,
 * ולא ניחוש שנשמע ודאי.
 */
export const SUBJECT_BY_FORM: Record<string, string> = {
  CUSTOMERS: 'הפנייה',
  ORDERS: 'ההזמנה',
  DOCUMENTS_D: 'תעודת המשלוח',
  AINVOICES: 'החשבונית',
  CINVOICES: 'החשבונית',
  EINVOICES: 'החשבונית',
  PORDERS: 'הזמנת הרכש',
  SERVCALLS: 'קריאת השירות',
};

/**
 * שם המסמך כפי שהלקוח יקרא אותו בתבנית: "מצורפת <סוג> מספר <מספר>".
 *
 * 🔴 **כל ערך כאן חייב להיות בלשון נקבה**, כי התבנית המאושרת פותחת ב"מצורפת"
 * והנוסח שלה קפוא אצל מטא. "מצורפת מסמך" יוצא ללקוח שבור, ואי אפשר לתקן
 * את התבנית בלי אישור מחדש. יש על זה בדיקה.
 *
 * 🔴 בנפרד מ-`SUBJECT_BY_FORM`, כי שם הניסוח מיודע ("תעודת המשלוח")
 * וכאן הוא חייב להיות סתמי, אחרת המשפט אצל הלקוח נשבר.
 */
export const DOC_TYPE_BY_FORM: Record<string, string> = {
  ORDERS: 'הזמנה',
  DOCUMENTS_D: 'תעודת משלוח',
  AINVOICES: 'חשבונית מס',
  CINVOICES: 'חשבונית מרכזת',
  // ✅ אומת חי 22/08/2026: התוסף למד `EINVOICES` ⟵ `WWWSHOWEIV` על המסך
  // של ר.שעל, ומספר המסמך שם הוא `OV2600987`.
  EINVOICES: 'חשבונית מס קבלה',
  PORDERS: 'הזמנת רכש',
  SERVCALLS: 'קריאת שירות',
};

/**
 * הקידומות של מספר המסמך פר מסך.
 *
 * 🔴 **הקידומות הן פר-לקוח**, בדיוק כמו שמות פרוצדורות ההדפסה ומיקומי
 * השדות. הטבלה הזאת נמדדה מהמחסן המסונכרן של ר.שעל ולא נוחשה, ואסור
 * להעתיק אותה ללקוח אחר בלי למדוד מחדש.
 *
 * מדידה (22/08/2026, `kukstfxtznymfkirdmty`):
 *   `SO` 3,044 הזמנות · `SH` 3,402 תעודות משלוח · `SC` 2,914 קריאות שירות
 *   `IN` 30 חשבוניות מס · `IK` 23 חשבוניות זיכוי · `SI` 2,996 חשבוניות מרכזות
 *
 * 🔴🔴 **`IK` אינו חשבונית מס, וזו המלכודת שהקובץ הזה נבנה סביבה.**
 * שני הסוגים יושבים באותו מסך `AINVOICES` ושניהם `IVTYPE='A'`, ולכן
 * לפי המסך לבדו הם נראים זהים. אבל **כל 23 שורות ה-`IK` הן בסכום שלילי**
 * (ממוצע 2,079 מינוס), כלומר אלה חשבוניות זיכוי. הודעה שאומרת "מצורפת
 * חשבונית מס" ומצורפת אליה חשבונית זיכוי היא אמירה שגויה ללקוח אמיתי,
 * ולכן סוג המסמך נגזר מהקידומת ולא מהמסך. ראה `DOC_TYPE_BY_PREFIX`.
 *
 * ⭐ **וזו לא חריגה של מסך אחד, זו תבנית.** נמדד על 109 אלף שורות בספר
 * הכספי של עוגן: לכל מסך חשבונית יש **סדרה חיובית וסדרת זיכוי לצידה**,
 * ושתיהן על אותו מסך ובאותו סוג מסמך:
 *   `IN` <bdi>37,598</bdi> חיוביות מול `IK` <bdi>5,707</bdi> **שליליות בכולן**
 *   `OV` <bdi>32,720</bdi> חיוביות מול `ON` <bdi>212</bdi> **שליליות בכולן**
 * 🔴 ולא כל קידומת שנייה היא סדרת זיכוי: `CR` (חשבונית מרכזת) יצא
 * **מעורב** (<bdi>207</bdi> שליליות מול <bdi>114</bdi> חיוביות), ולכן הוא
 * לא נכנס לכאן. סימן אינו ראיה עד שהוא נקי.
 */
export const DOC_PREFIX_BY_FORM: Record<string, string[]> = {
  ORDERS: ['SO'],
  DOCUMENTS_D: ['SH'],
  SERVCALLS: ['SC'],
  AINVOICES: ['IN', 'IK'],
  CINVOICES: ['SI'],
  // ⭐ `OV` נמדד חי על המסך של ר.שעל (`OV2600987`, 22/08/2026).
  // 🔴 והאות היא **O ולא ספרה אפס.** נבדק ב-`ascii()` על 32,720 שורות
  // בספר הכספי של עוגן, וכולן 79. בצילום מסך השתיים נראות זהות.
  // ✅ `ON` נוסף תחילה מהדפוס אצל עוגן בלבד, **ואומת אצל ר.שעל באותו יום**:
  // `ON2600058` נצפה על המסך לצד ארבע שורות `OV`. ההשערה החזיקה, אבל היא
  // הייתה השערה עד שנראתה.
  EINVOICES: ['OV', 'ON'],
};

/**
 * ⭐ **הקידומת יודעת יותר מהמסך.** מסך אחד יכול להחזיק שני סוגי מסמך
 * (`AINVOICES` מחזיק חשבונית מס וגם חשבונית זיכוי), ולכן כשהמספר שנבחר
 * נושא קידומת מוכרת, היא זו שקובעת את הכיתוב.
 */
export const DOC_TYPE_BY_PREFIX: Record<string, string> = {
  SO: 'הזמנה',
  SH: 'תעודת משלוח',
  SC: 'קריאת שירות',
  IN: 'חשבונית מס',
  IK: 'חשבונית זיכוי',
  SI: 'חשבונית מרכזת',
  OV: 'חשבונית מס קבלה',
  ON: 'חשבונית זיכוי',
};

/**
 * שתי אותיות ואחריהן ספרות.
 *
 * ⭐ **וזה גם מה שמסנן טיוטות, במקרה ולטובה.** מסמך בטיוטה בפריוריטי
 * מקבל מספר זמני של אות אחת ומספר רץ (`T14388` בחשבונית, `T20` בתעודת
 * משלוח, `T57247` בחשבונית מרכזת), והוא לא עומד בצורה הזאת. כלומר על
 * שורת טיוטה השדה נשאר ריק, והעובד נדרש להקליד. מסמך טיוטה אינו מסמך
 * שאמור לצאת ללקוח.
 */
export const DOC_NUMBER = /^[A-Z]{2}\d{5,9}$/;

export interface DocPrefill {
  subject: string;
  doc_type: string;
  doc_number: string;
  /**
   * true = אין לנו קידומות שנמדדו למסך הזה, ולכן בחירת המספר נשענת על
   * הכלל הכללי בלבד. משמש לרישום גילוי, לא לחסימה.
   * 🔴 נכון גם למסך שיש לו כיתוב אבל אין לו מדידה, כמו `EINVOICES`.
   */
  needs_measure: boolean;
}

/**
 * המסמך של השורה, מתוך כל הערכים שהתוסף ראה עליה.
 * **מחזיר מספר ריק כשיש ספק.**
 */
export function pickDocument(rawForm: unknown, candidates: string[]): DocPrefill {
  const form = String(rawForm ?? '').trim().toUpperCase();
  const shaped = [...new Set(candidates.filter((c) => DOC_NUMBER.test(c)))];

  const prefixes = DOC_PREFIX_BY_FORM[form];
  let number = '';
  if (prefixes) {
    const exact = shaped.filter((c) => prefixes.some((p) => c.startsWith(p)));
    // בדיוק אחד. שניים פירושם שהשורה מציגה שני מסמכים מאותה משפחה,
    // למשל חשבונית זיכוי לצד החשבונית שהיא מזכה, וזה לא מקרה לנחש בו.
    number = exact.length === 1 ? exact[0] : '';
  } else {
    // מסך שלא מיפינו: אין קידומת לסנן לפיה, ולכן רק מספר אחד בשורה כולה
    // נחשב ודאי. שניים ומעלה מחזירים ריק.
    number = shaped.length === 1 ? shaped[0] : '';
  }

  return {
    subject: SUBJECT_BY_FORM[form] ?? 'הפנייה',
    doc_type: DOC_TYPE_BY_PREFIX[number.slice(0, 2)] ?? DOC_TYPE_BY_FORM[form] ?? '',
    doc_number: number,
    needs_measure: !DOC_PREFIX_BY_FORM[form],
  };
}
