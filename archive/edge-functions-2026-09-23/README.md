# פונקציות קצה שנוטרלו בסקירת האבטחה, 23/09/2026

פונקציות שהיו פרוסות ב-Supabase בלי קוד בריפו. הוחלפו בגרסה שמחזירה 410 (לא נמחקו).

- `priority-cred-test`: בדיקה חד-פעמית מ-08/2026 של פרטי ההתחברות לפריוריטי. כל קריאה עם המפתח הציבורי התחברה לפריוריטי והחזירה הזמנה לדוגמה; קריאות חוזרות עלולות לנעול את החשבון.
- `priority-customer-webhook`: הוובהוק מתקופת Make/BPM (04/2026) שיצר שורה ב-`orders` על לקוח חדש. `verify_jwt=false` וסוד כתוב בגלוי בקוד (הוסר מהעותק כאן). הסנכרון בקוד מחליף אותו מ-04/08.
  - הקוד: בדק טוקן Bearer מול קבוע, חיפש הזמנה לפי טלפון ואם אין, הוסיף `orders` עם שם, טלפון, קופה (MCUSTDES), פותח ו"ממתין לתאום".
- `zzdump`: כבר נוטרל קודם (410), לא נגעו.
