# תזכורת למחר — 05/07/2026 · rashal-dashboard

## 🎯 המשימה: בדיקה חיה של דחיפת הצ'אט לפריוריטי, ואז אישור הפעלה

### מה עשינו אתמול (04/07)
מנגנון שלם שמדחיף את הצ'אט של אנשי השטח (טקסט + תמונות) לכרטיס הלקוח בפריוריטי.
פוצחנו את מנגנון הנספחים (data URI ב-EXTFILENAME). נבדק על 2 לקוחות, עובד. חסם הכפילות נסגר.

### מצב התרחישים ב-Make (ארגון Mishal, גישה דרך `~/Idan-HQ/knowledge/client-access/rashal-make.sh`)
- 🟢 `OGEN - rashal-priority-pull` (9479125) — משיכה חול, חי, ראשון 07:00
- 🟢 `OGEN - rashal-priority-pull-weekend` (9479194) — משיכה סופ"ש, חי
- ⚪ `OGEN - rashal-chat-push` (9479946) — **כבוי**, פרודקשן מוכן, מחכה לאישור

### צעדי הבדיקה החיה מחר
1. עידן: פותח את הדשבורד, בוחר לקוח, מוסיף הודעת צ'אט + מעלה תמונה.
2. עוגן: מריץ את הדחיפה ממוקד ללקוח הזה (hook בדיקה: `?test_custname=<CUSTNAME>` על ה-endpoint / התרחיש).
3. מאמתים בכרטיס הלקוח בפריוריטי: ההודעה בדו-שיח הפנימי + התמונה כנספח JPG.
4. אם תקין → עידן מאשר → מדליקים את התרחיש + תזמון.

### החלטות שממתינות לעידן (אחרי שהבדיקה עוברת)
1. **18 הודעות צ'אט ישנות ממתינות** — קדימה-בלבד (לסמן כטופלו) או לדחוף גם אותן?
   אם דוחפים → קודם **דחיסה רטרואקטיבית** לתמונות הישנות ב-bucket (הן ~2.3MB, מלפני הדחיסה).
2. **תדירות הדחיפה** כשמדליקים (כרגע interval 900 = 15 דק', sequential).

### 🧹 ניקוי ידני (המעטפת חוסמת DELETE)
בכרטיס לקוח **301114658 (פיטוסי יצחק)** בפריוריטי:
- 3 שורות דו-שיח בדיקה (`[בדיקת עוגן...]`)
- 4 נספחי PNG בדיקה (`בדיקה-עוגן`)
בכרטיס **015389406 (שמשוביץ ויויאן)**: התמונה האמיתית נכנסה פעמיים — עותק אחד למחיקה.
→ למחוק ידנית ב-UI של פריוריטי.

### תוכנית מלאה
`docs/CHAT-TO-PRIORITY-PLAN.md` · לקחי Priority: `~/Idan-HQ/knowledge/priority/learnings.md`
